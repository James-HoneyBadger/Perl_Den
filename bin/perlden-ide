#!/usr/bin/perl
# ============================================================================
# PerlDen IDE - Linux Sysadmin Toolkit & Perl Development Environment
# ============================================================================
use strict;
use warnings;
use utf8;
use FindBin qw($RealBin);
use lib "$RealBin/../lib";
use Getopt::Long;

my $show_help;
my $show_version;
Getopt::Long::Configure('pass_through');
GetOptions(
    'help|h'    => \$show_help,
    'version|v' => \$show_version,
);

if ($show_help) {
    print <<'HELP';
Usage: perlden-ide [OPTIONS] [FILE ...]

Launches the PerlDen IDE - a GTK3-based Perl development environment
with integrated Linux system administration tools.

Options:
  -h, --help       Show this help message
  -v, --version    Show version number

Files passed on the command line will be opened in editor tabs.
HELP
    exit 0;
}

use PerlDen::App;

if ($show_version) {
    print "PerlDen IDE v$PerlDen::App::VERSION\n";
    exit 0;
}

my $app = PerlDen::App->new;
$app->{open_files} = [@ARGV] if @ARGV;  # Pass files to open
$app->run;

__END__

=encoding utf8

=head1 NAME

perlden-ide - PerlDen IDE launcher

=head1 SYNOPSIS

    perlden-ide [OPTIONS] [FILE ...]

=head1 DESCRIPTION

Launches the PerlDen IDE — a professional GTK3-based Perl development
environment with integrated Linux system administration tools.  Files
passed on the command line are opened as editor tabs on startup.

=head1 OPTIONS

=over 4

=item B<-h>, B<--help>

Print a usage summary and exit.

=item B<-v>, B<--version>

Print the version number and exit.

=back

=head1 SEE ALSO

L<PerlDen::App>, L<perlden-cli>, L<perlden-tui>

=head1 AUTHOR

James

=head1 LICENSE

MIT

=cut
